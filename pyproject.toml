[project]
name = "ai-stock-analyzer"
version = "0.1.0"
description = "A local A-share quantitative stock analysis tool."
requires-python = ">=3.11"

[project.scripts]
ai-stock = "src.cli.main:cli"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
